name: My first workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkout@v6   

        - name: welcome msg
          run: echo "first actions job"
          
        - name: List files
          run: ls

        - name: Read file
          run: cat README.md  
        
          